# moh-ami: Solving a Friend's Problem

moh-ami (pronounced "moh-ah-mee", from "mot ami" meaning "word friend") is a French learning translation tool that I built to solve a personal challenge while demonstrating end-to-end LLM integration skills. It's a production-deployed application that goes beyond simple translation to provide detailed educational explanations for language learners.

The core concept is that simple translation tools don't help you understand why words are translated certain ways or what grammar rules apply. If you just see "Comment allez-vous?" translates to "How are you?", you don't learn why "allez" is used instead of "aller" or why "vous" can be formal "you" instead of plural. moh-ami provides word-by-word mappings, grammar rule explanations, cultural context notes, and alternative translations with explanations of differences.

I started this project while learning French myself and realizing that existing translation tools weren't helping me actually learn the language. I wanted to build something that explained the translation process, not just showed the result. This became an opportunity to demonstrate full-stack development with modern technologies and LLM integration in a production application.

What makes moh-ami special to me is the direct personal connection - I was solving a real problem for someone I care about, which gave the work genuine meaning beyond just building something for my portfolio. The tool has been shared with friends learning French, and their feedback validated the educational value. Knowing real people find it useful beyond just being a portfolio piece makes the project more meaningful.

The technical implementation uses Next.js 14 with the App Router, GraphQL API with Apollo Server, PostgreSQL database via Prisma ORM, and OpenAI GPT-4o-mini for the LLM integration. The frontend is React with TypeScript, Redux Toolkit for state management, and Tailwind CSS for styling. The system features synchronized side-by-side text comparison with interactive chunk selection, hover highlighting, and expandable explanation panels.

What makes moh-ami special is the structured LLM integration. I designed careful prompts that request specific JSON schema outputs, which makes the responses consistent and parseable. The system does semantic chunking where text is split into meaningful units (50-150 characters) rather than word-by-word, which produces more coherent explanations. There's validation logic that catches common LLM errors before they reach users, and comprehensive error handling for API failures, quota limits, rate limits, and context length issues.

The personal utility aspect is important - I actually use moh-ami when reading French text or practicing translations. Having an AI explain the grammar and context helps me learn more effectively than just seeing translations. This means the project provides genuine value beyond being a portfolio piece, which keeps me engaged with maintaining and improving it.

From a portfolio perspective, moh-ami demonstrates several valuable skills. It shows I can integrate LLMs into production applications with proper prompt engineering and error handling. It proves I understand full-stack development with modern frameworks like Next.js 14 and GraphQL. It shows I can design interactive user experiences with complex state management. Most importantly, it demonstrates I can build complete systems from concept to deployed application that solve real problems.

The project went through significant iteration during development. I started with complex text matching algorithms and simplified to ID-based chunk selection. I moved from eager to lazy initialization for OpenAI client to handle deployment compatibility. I evolved from generic prompts to structured JSON responses with validation. These pivots demonstrate adaptability and the ability to recognize when simpler approaches work better than complex ones.

The chunk selection debugging was particularly educational. Initially, only the first chunk was selectable, and I suspected it was because other chunks didn't have explanations. I had been overcomplicating the explanation-to-chunk mapping with multiple text matching methods. The breakthrough came when I simplified to an ID-based approach where each explanation has an ID (its index in the array) and chunks are matched to explanations using character positions from OpenAI's response. This taught me that when a solution feels too complex, it probably is - the right answer is often simpler than the first approach. The deployment to Railway revealed another critical learning: environment variables aren't available during build time, only at runtime. I had to change from eager initialization (checking for OPENAI_API_KEY at module load) to lazy initialization (checking only when the translation function is called). This pattern is essential for cloud deployments and taught me to think about build-time vs runtime behavior.

The most challenging and rewarding aspect was crafting the LLM prompt. Getting the model to consistently produce the structured output I needed - synchronized translations, grammatical insights, cultural nuances - required iteration and precision. This is an area where I have significant skill and experience, and moh-ami really showcases that ability to engineer effective prompts that extract exactly what you need from language models.

What I'm particularly proud of is that moh-ami is real and deployed. You can visit the live application, enter text, and see it working. The source code is on GitHub showing the implementation details. This transparency means potential employers can verify everything I claim about the project by actually using it and reviewing the code.

The project was built over approximately 3-4 weeks of focused development, with the initial implementation happening in a single intensive session on January 11, 2026. This rapid development timeline demonstrates I can deliver complete, deployed applications in reasonable timeframes rather than spending months on perfect implementations. Balancing quality with delivery speed matters in professional contexts.

The rapid build required making key technical decisions quickly. I chose Next.js 14 App Router over Pages Router for modern React patterns, GraphQL over REST for flexible data fetching, and Redux Toolkit for centralized state management. The LLM integration approach was to send the entire English text to OpenAI in one request with structured JSON prompts, rather than line-by-line processing. This was more efficient and allowed the LLM to understand context better. The text splitting strategy grouped consecutive short sentences (under 80 characters) together until reaching 150 characters maximum, creating more semantically coherent chunks for line-by-line comparison. These decisions were made based on understanding the requirements and choosing the right tools for the job, not just following tutorials.

Operating moh-ami costs under $10/month - about $5 for Railway hosting and $1-2 for OpenAI API usage based on my personal usage patterns. This proves I understand cost implications of technical decisions and can build efficient systems. For professional applications, I'd apply similar cost-awareness at larger scales.

The codebase is clean, well-organized, and maintainable. TypeScript throughout provides safety. Components are reasonably sized and focused. Business logic is separated from presentation. Error handling is comprehensive. These quality markers make the code sustainable over time rather than just working initially.

The GitHub repository includes comprehensive README with setup instructions, deployment procedures, and API documentation. This shows I understand that code is only part of the deliverable - documentation enables others to understand and contribute. Professional development includes communication about code, not just code itself.

Within my portfolio, moh-ami complements WhatNow by demonstrating different technical approaches. WhatNow focuses on recommendation algorithms and learning systems with contextual bandits. moh-ami focuses on LLM integration and interactive UI. Together they show breadth - I can work with different types of AI systems, different architectural patterns, and different problem domains.

The project has clear paths for expansion if desired. User authentication would enable saved translation history. Multiple language pairs would broaden utility. Pronunciation audio would enhance learning. Grammar quizzes could make it more interactive. The current architecture supports these additions without major refactoring. Having a clear roadmap demonstrates I think about long-term maintainability.

For employers evaluating my capabilities, moh-ami demonstrates readiness for full-stack development roles involving modern web technologies and AI integration. The project is evidence of skills rather than just claims. The transparency of deployed application and public source code means everything is verifiable. This completeness and transparency make moh-ami one of my strongest portfolio pieces.

moh-ami proves I can take projects from concept through production deployment, integrate modern AI technologies effectively, build sophisticated user interfaces, implement full-stack systems with type safety, solve technical challenges across the stack, manage project scope and priorities, maintain code quality while delivering features, and create applications that provide genuine utility beyond just portfolio pieces.

