# Railway project configuration
# This file helps Railway understand the monorepo structure
# Each service needs its root directory set in the Railway dashboard

[build]
builder = "NIXPACKS"

# Note: Root directories must be set in Railway dashboard:
# - Backend service: rootDirectory = "backend"
# - Frontend service: rootDirectory = "frontend"

